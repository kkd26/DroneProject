There is a bug in Olympe causing errors when streaming video.
To fix it, run the command below.

```
git -C ~/code/parrot-groundsdk/packages/olympe am < 0001-DEBUG-fix-arsdkng-pdraw-video-streaming.patch
```
